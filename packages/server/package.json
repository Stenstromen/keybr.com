{
  "private": true,
  "name": "@keybr/server",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@keybr/assets": "*",
    "@keybr/config": "*",
    "@keybr/intl": "*",
    "@keybr/logger": "*",
    "@keybr/names": "*",
    "@keybr/pages-server": "*",
    "@keybr/pages-shared": "*",
    "@keybr/rand": "*",
    "@keybr/themes": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "tstest --import=@keybr/test-env-server --import=@keybr/test-env-bundler --test --test-concurrency=1 'lib/**/*.test.{ts,tsx}'",
    "fast-test": "tstest --import=@keybr/test-env-server --import=@keybr/test-env-bundler --test 'lib/**/*.test.{ts,tsx}'"
  }
}
